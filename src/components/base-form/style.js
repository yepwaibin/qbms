import styled from "styled-components";

export const BaseFormWrapper = styled.div`
  margin-top: 20px;
  .deleteButton {
    position: absolute;
    right: 0;
  }
  .radioOptions {
    width: 100%;
  }
`;
